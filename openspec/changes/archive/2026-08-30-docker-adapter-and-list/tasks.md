# Tasks

- [x] Adapter for the daemon over the API, one query for the inventory
- [x] Resolve the endpoint from the Docker context, not only from the environment
- [x] The grouping of containers into environments as a use case, tested against fakes
- [x] `hm list` in Go, text and JSON, with the same exit codes
- [x] Sort in the C locale in the shell implementation, so both agree whatever the machine
- [x] Integration test comparing both implementations, document and table
- [x] `MIGRATION.md` updated
