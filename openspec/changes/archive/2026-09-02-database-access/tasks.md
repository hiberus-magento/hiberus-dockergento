# Tasks

- [x] A capturing runner over the daemon's exec, beside the attaching one
- [x] `Query` in the engine, finding the container by label
- [x] The statement carried in the environment, and the client chosen inside the container
- [x] `hm mysql`: statement, session, dump on the input, and the refusals
- [x] The import routed to the shell implementation, by one tested condition
- [x] Unit tests with no daemon, and an integration test against a real database
- [x] `MIGRATION.md` updated
