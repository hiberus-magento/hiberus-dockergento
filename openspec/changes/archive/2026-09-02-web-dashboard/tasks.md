# Tasks

- [x] HTTP adapter over the facade, answering the documents the command line prints
- [x] Loopback binding, a token on every request, and the Host check
- [x] The dashboard page, embedded, with no build step
- [x] `serve up | down | status`, detached, waiting until it answers
- [x] One command from either entry point, and exempt from the project check
- [x] Parse the tool's own flags wherever they appear, with the transparent-command rule
- [x] Draw the rule around the project name, as the shell implementation does
- [x] Decide log colour the way Compose decides it
- [x] Integration tests for the dashboard, and a guard for the flag defect
- [x] `MIGRATION.md` and the command descriptions updated
