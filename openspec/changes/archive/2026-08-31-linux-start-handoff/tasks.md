# Tasks

- [x] `post-start` as a command, guarded on the platform, called by both implementations
- [x] `start` and `restart` dispatched to Go on every platform
- [x] Stop demanding a TLS terminator a project on the proxy does not have
- [x] Tests: the hand-off happens on Linux, does not on macOS, and the steps live in one place
- [x] `MIGRATION.md` updated
