# Tasks

- [x] The installation, described the way the shell implementation describes it
- [x] The Docker and Compose underneath, with "not available" as an answer
- [x] Which build of the binary is running, as a field of it
- [x] `hm _binary` retired, and the tests that used it pointed at `version`
- [x] Compared against the shell implementation
- [x] `MIGRATION.md` updated
