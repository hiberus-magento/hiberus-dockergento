# Tasks

- [x] `console/tasks/anonymisation.sh`: read, record and clear the state
- [x] Only ask Docker for a terminal when there is one
- [x] Clear the state in `db restore`, `db clone`, `mysql -i` and `transfer-db`
- [x] Record it after a successful `hm masquerade`
- [x] `hm worktree add --profile=agent` anonymises by default; `--no-anonymise` skips
- [x] `hm describe --json` reports the state
- [x] The generated agent context says it, in words
- [x] A doctor check for an agent's environment that was never anonymised
- [x] Unit and integration tests
- [x] Documentation, changelog and backlog
