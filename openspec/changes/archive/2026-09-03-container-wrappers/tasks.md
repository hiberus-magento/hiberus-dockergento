# Tasks

- [x] The eight wrappers, through one call into the container
- [x] `mysqldump` through the capture that keeps the streams apart
- [x] Varnish on and off, with the page cache and the VCL agreeing
- [x] Fix `test-unit` in the shell implementation, which never ran the tests
- [x] Compared against the shell implementation over a real environment
- [x] `MIGRATION.md` updated
