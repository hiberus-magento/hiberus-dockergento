# Tasks

- [x] `list` and `remove` in Go, against the registrations the shell implementation writes
- [x] The state read from the containers and the directory, never from the registration
- [x] The refusals kept: no name, no such environment, uncommitted changes
- [x] The environment taken down with its volumes, the worktree removed, git pruned
- [x] Integration test comparing both implementations, including a directory that is gone
- [x] `add` left in shell, with a test that it still gets there
- [x] `MIGRATION.md` updated, with the storage swap recorded as its own step
