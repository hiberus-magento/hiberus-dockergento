# Tasks

- [x] Progress and asking as engine options, implemented by the command line
- [x] A one-off container runner, for the anonymiser and for `masquerade` after it
- [x] Feeding a stream into a container, so a dump never exists twice
- [x] The import: clauses, contents, the record, the anonymiser, the store's addresses
- [x] Fix `-d` taking an argument it does not have
- [x] Unit tests for the stripping and the domain, integration tests against a real database
- [x] `MIGRATION.md` updated
