# Tasks

- [x] `--write` flag on `hm mcp`, and on `hm mcp --config`
- [x] Write tool definitions, added to the catalogue only when enabled
- [x] Annotations on every tool, read and write
- [x] `cache_clean`, `cache_flush`, `reindex`, `config_set`
- [x] A configuration path is checked before it is used
- [x] Unit tests: the path rule, and the catalogue with and without writes
- [x] Integration test: listing, calling, and the refusal when read-only
- [x] `docs/mcp.md`, changelog, backlog
