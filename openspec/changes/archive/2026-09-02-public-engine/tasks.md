# Tasks

- [x] Move core, ports, app and adapters out of `internal/`
- [x] The facade: options, defaults, and the calls the tool answers
- [x] Move what the command line knew about projects into the engine
- [x] The command line imports the facade and the domain types, and nothing else
- [x] Integration test: a separate module builds against it, asks and acts
- [x] Assert the command line stays private
- [x] Architecture document and `MIGRATION.md` updated
