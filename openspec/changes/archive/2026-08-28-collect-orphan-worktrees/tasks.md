# Tasks

- [x] `hm clean` reads the worktree registry and lists the orphans
- [x] `--force` removes their containers and volumes by name, and forgets the registration
- [x] They appear in the JSON report
- [x] `hm worktree list` points at `hm clean` when something is missing
- [x] Integration test: an orphan is collected, a live one is not
- [x] Documentation, changelog and backlog
