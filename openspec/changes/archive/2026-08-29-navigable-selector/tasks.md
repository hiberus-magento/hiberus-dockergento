# Tasks

- [x] `hm_select_move` and `hm_select_render`, with no terminal involved
- [x] The arrow selector over the existing terminal primitives
- [x] `fzf` when it is installed; the numbered list when nothing can be drawn
- [x] `custom_select` keeps its signature and its `REPLY`
- [x] Unit tests for the movement and the rendering
- [x] Integration test through a pseudo-terminal: arrows, digits, Enter
- [x] Documentation, changelog and backlog
