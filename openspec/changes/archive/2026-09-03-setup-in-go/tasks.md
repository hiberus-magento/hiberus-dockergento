# Tasks

- [x] The options, with the spellings people already use, refused before anything is created
- [x] The questions, and which of them can be answered without asking
- [x] The properties, merged rather than replaced, with the name recorded only when it is a decision
- [x] The compose files from the templates, and the machine overlay's bind mounts
- [x] The proxy overlay, compared byte for byte with the shell implementation's, and removed when
      the project stops using the proxy
- [x] The installation, in the order the shell implementation runs it
- [x] `MIGRATION.md` updated
