# Tasks

- [x] `console/helpers/digest.sh`: a digest that works on both platforms, over files and directories
- [x] `add_registration_entry` records a real digest and the origin
- [x] `hm ai-pull` sets the origin for every resource it installs
- [x] `console/commands/ai-doctor.sh` with the five states and `--json`
- [x] Register the command and exempt it from the Docker validation
- [x] Integration test: current, modified, custom and missing, and a real recorded digest
- [x] Documentation, changelog and backlog
