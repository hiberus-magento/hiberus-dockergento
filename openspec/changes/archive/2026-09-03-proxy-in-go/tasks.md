# Tasks

- [x] The compose file, byte for byte, written only when it changed
- [x] Up, down and status, with the refusals the shell implementation gives
- [x] The routes, from the proxy's own API
- [x] The container holding the ports, named the same way by both halves
- [x] Starting an environment that needs the proxy stops going through the shell
- [x] Compared against the shell implementation, with the parts that need the ports skipped where
      something is already holding them
- [x] `MIGRATION.md` updated
