# Tasks

- [x] `magento` in Go, with the Magento CLI's own exit code
- [x] `composer` in Go, with both refusals through the error contract
- [x] The macOS vendor mirror left whole with the shell implementation, behind one tested condition
- [x] Release workflow on macOS, building all four binaries before publishing
- [x] Build and run each of the four, on its own platform
- [x] Tests for a project with the proxy and one without
- [x] `MIGRATION.md` updated
