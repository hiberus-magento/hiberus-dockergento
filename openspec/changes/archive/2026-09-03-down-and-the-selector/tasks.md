# Tasks

- [x] The selector: fzf, the arrow keys, the numbered list, and the refusal
- [x] It is handed to the engine, beside the question that asks for a value
- [x] `down`, with the flags Compose's own `down` takes
- [x] The three answers, and the copy taken before destroying anything
- [x] Removing orphans becomes something the caller asks for
- [x] Compared against the shell implementation
- [x] `stop --snapshot` takes the copy directly, instead of asking the shell half for it
- [x] An aborted question reported the way a shell reports one
- [x] `MIGRATION.md` updated
