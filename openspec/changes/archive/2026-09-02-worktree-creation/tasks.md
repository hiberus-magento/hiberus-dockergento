# Tasks

- [x] The naming: what a branch becomes, the profiles, and the service each is reached through
- [x] The overlay generator, pure, tested, and diffed against the shell implementation's output
- [x] Prepare, create and settle, with every refusal before anything exists
- [x] The lock the shell implementation takes, taken the same way, newline and all
- [x] Dependencies mounted read-only, and only while the locks match
- [x] The data cloned from the project's template, and anonymised on the agent profile
- [x] Integration test comparing the registration, the overlay and every refusal
- [x] `MIGRATION.md` updated, with the storage swap as the next step
