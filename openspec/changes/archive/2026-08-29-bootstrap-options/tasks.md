# Tasks

- [x] `console/tasks/setup_options.sh`: parse and validate, assigning nothing else
- [x] Long forms, `--option value`, and the Warden aliases
- [x] A missing dump is a usage error before anything is created
- [x] `hm setup --yes --clean-install` asks nothing
- [x] `hm install --use-default`
- [x] Declare the new option names where every option is declared
- [x] Unit tests for the parser; integration test for the refusals
- [x] Documentation, changelog and backlog
