# Tasks

- [x] Read the Compose configuration with `compose-go`, checked against the command
- [x] `hm describe` in Go: document, table, credentials, exit codes
- [x] Ask the six independent questions at once
- [x] Merge the tool's defaults into the properties, as the shell implementation does
- [x] Adapters for the project's files, the tooling versions and the anonymisation state
- [x] Integration test comparing both implementations
- [x] `MIGRATION.md` updated, with what each ported command costs
