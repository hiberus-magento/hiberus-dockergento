# Tasks

- [x] Measure whether the library is usable, whether what it creates is the same, and what it saves
- [x] Add the dependency, pinned to the versions compose itself requires
- [x] Read the worktree registry from the Go layer
- [x] Correct the compose file list: proxy overlay, worktree overlay, and what a description reports
- [x] Orchestrator port and the Compose adapter, with Compose's own progress and log consumers
- [x] The operator: the proxy, the bind-mount refusal, the snapshot, the restart
- [x] `start`, `stop`, `restart`, `logs`, `exec`
- [x] Unit tests for the decisions, with no daemon
- [x] Integration test for the property the whole decision rests on
- [x] `MIGRATION.md` and the architecture document updated, ADR-009 revised
