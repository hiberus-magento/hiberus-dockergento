# Tasks: The two docker tools stop going through bash

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ≈970–1090 total (see per-file breakdown below), measured with `git diff --shortstat 382247c..HEAD -- . ':(exclude)openspec'` |
| 400-line budget risk | High |
| Chained PRs recommended | No — `delivery_strategy: single-pr`, no PRs open; the orchestrator commits directly on `release/2.0.0` |
| Suggested split | Two work-unit commits, each self-consistent, each carrying its own tests, MIGRATION row and deletion |
| Delivery strategy | single-pr |
| Chain strategy | size-exception |

Decision needed before apply: No
Chained PRs recommended: No
Chain strategy: size-exception
400-line budget risk: High

The `size:exception` was accepted by the user up front (design's Size section). Nothing is cut to
fit the budget; the two-commit split exists purely to keep each commit a reviewable slice.

### Lines per file group (from design's Size section, additions + deletions)

| Commit | Group | Lines |
|---|---|---|
| 1 · `docker-stop-all` | production (`ports.go`, `engine.go` adapter, `core/orchestration.go`, `orchestrate.go`, `dockergento.go`, `internal/cli/*`) | ≈200 |
| 1 | Go tests (six app tests, routing test, CLI contract tests) | ≈300 |
| 1 | `console/commands/docker-stop-all.sh` stub | +18/−51 |
| 1 | `tests/integration/stop_all_guard_test.sh` deleted | −92 |
| 1 | **Commit 1 subtotal** | **≈610–670** |
| 2 · `docker-compose` | production (port, `Operator.Compose`, `composecli/runner.go`, facade) | ≈135 |
| 2 | Go tests (`composeRunner` fake, CLI contract tests, terminal probe) | ≈170 |
| 2 | `console/commands/docker-compose.sh` stub | +18/−4 |
| 2 | probe swap + `go_passthrough_test.sh` compose cases | ≈40 |
| 2 | **Commit 2 subtotal** | **≈360–420** |
| — | **Total** | **≈970–1090** |

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|---|---|---|---|---|---|
| 1 | `docker-stop-all` fully in Go, `start -s` rewired off `Legacy.Run` | Commit 1 (direct to `release/2.0.0`) | `go test ./internal/cli ./dockergento/... -short -run 'DockerStopAll\|StopEverything\|ConfirmingStopsThem\|SomeContainersRefuseToStop\|StartingWithMinusS\|AnAPICallerIsNeverAsked\|StoppingTheRest\|TheIdsAskedFor\|TheDoesNotBelongLine\|TheRouterAnswers'` | `go test ./test/e2e/... -run StopAll` (default subset; confirmed-stop subset only with `HM_E2E_STOP_ALL=1`, opt-in only) | Revert `dockergento/ports/ports.go` (`Stop`), `dockergento/adapters/dockerd/engine.go` (`Stop`), `dockergento/core/orchestration.go` (`MachineStop`), `dockergento/app/orchestrate.go` (`Ask`, `StopEverything`, `Start`'s rewiring), `dockergento/app/environments_test.go`, `dockergento/app/orchestrate_test.go`, `dockergento/dockergento.go` (`StopEverything`, `Ask` wiring, `StartOptions.Interactive`), `internal/cli/engine.go`, `internal/cli/orchestrate.go`, `internal/cli/docker_stop_all.go`, `internal/cli/docker_stop_all_test.go`, `internal/cli/routing_test.go`, `internal/cli/run.go`'s `docker-stop-all` case, `console/commands/docker-stop-all.sh`, restore `tests/integration/stop_all_guard_test.sh`, `MIGRATION.md` row, `test/e2e/stop_all_test.go` |
| 2 | `docker-compose <args>` passthrough in Go | Commit 2 (direct to `release/2.0.0`) | `go test ./internal/cli ./dockergento/... -short -run 'DockerCompose\|ComposeExitCode\|ComposeRunnerIsAskedToRun\|AMissingComposeBinary\|TheRouterAnswers'` | `tests/integration/go_passthrough_test.sh` (compose cases + terminal-stream case) | Revert `dockergento/ports/ports.go` (`ComposeRunner`), `dockergento/adapters/composecli/runner.go`, `dockergento/core/environment.go` (`ComposeFiles.Paths`), `dockergento/adapters/composelib/orchestrator.go`, `dockergento/app/orchestrate.go` (`Compose`), `dockergento/dockergento.go` (`Compose` facade), `internal/cli/engine.go`, `internal/cli/docker_compose.go`, `internal/cli/docker_compose_test.go`, `internal/cli/routing_test.go`'s compose case, `internal/cli/run.go`'s `docker-compose` case, `console/commands/docker-compose.sh`, `tests/integration/worktree_environments_test.sh:284` probe, `tests/integration/go_passthrough_test.sh` compose cases, `MIGRATION.md` row — independent of Commit 1 |

## Phase 1: Commit 1 — `docker-stop-all` (RED → GREEN)

- [x] 1.1 Foundation (no RED expected — interface + adapter must compile together): add `Stop(ids []string) (failed []string, err error)` to `ports.ContainerEngine` (`dockergento/ports/ports.go:80-89`); implement `dockerd.Engine.Stop` beside `Remove` (`dockergento/adapters/dockerd/engine.go:108`) via `ContainerStop`/`container.StopOptions{}`, 30s deadline per container, bounded concurrency 8, `failed` rebuilt in input order; extend the `engine` fake in `dockergento/app/environments_test.go:10-17` with a `Stop` that records the ids it was asked and returns a configurable `failed` slice; add `core.MachineStop{Total, Others, Stopped}` beside `DownOptions` in `dockergento/core/orchestration.go`.
  Verify: `go build ./...` succeeds (interface, adapter and fake stay in sync); `gofmt -l ./cmd ./internal` empty.
- [x] 1.2 App RED: in `dockergento/app/orchestrate_test.go`, add `TestConfirmingStopsThem`, `TestTheIdsAskedForAreTheIdsFoundRunning`, `TestTheDoesNotBelongLineIsConditional` (recording `Announce`), `TestSomeContainersRefuseToStop`, `TestStartingWithMinusSAsksTheSameQuestion`, `TestAnAPICallerIsNeverAsked`, `TestStoppingTheRestBeforeStarting`, `TestStoppingTheRestIsTheSameCallOnEitherPlatform` (same assertion with `Platform` set to `mac` and `linux`) — all calling `Operator.StopEverything` and `Operator.Ask`, neither of which exists yet.
  Verify RED: `go test ./dockergento/app -short -run 'TestConfirmingStopsThem|TestTheIdsAskedForAreTheIdsFoundRunning|TestTheDoesNotBelongLineIsConditional|TestSomeContainersRefuseToStop|TestStartingWithMinusSAsksTheSameQuestion|TestAnAPICallerIsNeverAsked|TestStoppingTheRestBeforeStarting|TestStoppingTheRestIsTheSameCallOnEitherPlatform'` → **FAILS to compile**: `Operator.StopEverything` and `Operator.Ask` are undefined.
- [x] 1.3 App GREEN: add `Ask func(question, suggestion string) (string, error)` to `Operator` (`dockergento/app/orchestrate.go`, beside `Choose`); implement `Operator.StopEverything` — filters `Containers()` to `Running`, counts mine/others by `ComposeProject != project.Name`, announces the five sentences through `o.say`, asks `"Stop them all? [y/N]:"` (no trailing space; `prompt()` adds it) only when `interactive`, accepts only `y`/`Y`, calls `refuseFromAnUnregisteredWorktree` first, stops via `o.Engine.Stop`, and a decline is not an error; rewire `Operator.Start`'s `stopOthers` branch (`orchestrate.go:63-67`) off `o.Legacy.Run` onto `o.StopEverything`.
  Verify GREEN: `go test ./dockergento/app -short -v` → **PASSES**, all eight new tests and the existing package baseline.
- [x] 1.4 Facade + wiring: add `Engine.StopEverything(dir string, interactive bool) (core.MachineStop, error)` in `dockergento/dockergento.go`; wire `Ask: e.options.Ask` into `operator()` (`dockergento.go:1337`), nil-guarded like `Choose`; add `Interactive bool` to `StartOptions` (`dockergento.go:201-208`); set it at `internal/cli/orchestrate.go:36-37` as `Interactive: os.Getenv("HM_NON_INTERACTIVE") == ""`, exactly as `internal/cli/down.go:59`; confirm `internal/api/server.go:185-188` builds `StartOptions` without `Interactive` (leaves it `false` by omission — no code change there).
  Verify: `go build ./...` succeeds; `go test ./dockergento/... -short` green.
- [x] 1.5 CLI RED (contract): create `internal/cli/docker_stop_all_test.go` with `TestWhatDockerStopAllAsks` (seam records the request for the resolved directory carrying whether the run is interactive, and nothing else), `TestAFailedStopIsReportedAsADockerFailure` (exit 3, `docker_failed`, ids named), `TestDockerStopAllAnswersADocumentWhenNobodyIsWatching` (JSON `{"total","others","stopped"}` off-terminal). Handler body starts as `return exitError`, expected call log `[{Method:"Resolve"}, {Method:"StopEverything"}]` empty.
  Verify RED: `go test ./internal/cli -short -run TestWhatDockerStopAllAsks` → **FAILS**: the handler does not exist / calls nothing, so the recorded call log stays empty against the expected one.
- [x] 1.6 CLI RED (routing): create `internal/cli/routing_test.go` with `TestTheRouterAnswersForTheDockerTools`, asserting `cli.Run([]string{"docker-stop-all", "--yes"}, ...)` records nothing.
  Verify RED: `go test ./internal/cli -short -run TestTheRouterAnswersForTheDockerTools` → **FAILS for the right reason**: no `case "docker-stop-all"` in `run.go` yet, so the call falls through to `engine(...).Shell`, which execs the pinned `HM_LEGACY_ROOT/bin/run` and returns 3 — writing only this test would stay green regardless of what the handler asks, which is why 1.5 exists too.
- [x] 1.7 CLI GREEN: extend `commands` in `internal/cli/engine.go` with `StopEverything(dir string, interactive bool) (core.MachineStop, error)`; extend `call` with `Interactive bool`; make `fakeEngine.StopEverything` answer from a `stopped core.MachineStop` field and consume an outcome for the error, the way `Resolve` answers from `project`; create `internal/cli/docker_stop_all.go`: handler resolves the project, calls `StopEverything`, prints the JSON document only, uses `report()` for errors; add `case "docker-stop-all": return dockerStopAll(rest[1:], stdout, stderr, jsonOutput)` in `internal/cli/run.go`.
  Verify GREEN: `go test ./internal/cli -short -v` → **PASSES**, all new tests from 1.5 and 1.6, plus the full pre-existing package.
- [x] 1.8 Shell delegation + cleanup: rewrite `console/commands/docker-stop-all.sh` as a `console/commands/worktree.sh:18-26`-style stub (read-only reference) — the `binary_missing` refusal, then `exec "$binary" docker-stop-all "$@"`; leave `console/commands/start.sh:11` (read-only) untouched — it already calls the stub; delete `tests/integration/stop_all_guard_test.sh` (it proves logic that no longer lives in bash).
  Verify: `tests/run.sh unit` still passes (guard suite's assertions are gone, not replaced 1:1 in bash); `bash -n console/commands/docker-stop-all.sh` parses.
- [x] 1.9 Integration: in `tests/integration/go_passthrough_test.sh`, add the case "the shell entry point still answers for docker-stop-all" (with nothing running, through `bin/run`, reaching the binary through the stub) and the case "the question is asked on the caller's own terminal" (`HM_NON_INTERACTIVE=` cleared, `printf 'n\n' | bin/run docker-stop-all`, exit 0, "Nothing was stopped." or "No containers running").
  Verify: `tests/run.sh tests/integration/go_passthrough_test.sh` passes on a machine with Docker; skipped gracefully otherwise per the suite's own guard.
- [x] 1.10 e2e (deferrable slice — see Phase 1 note below): create `test/e2e/stop_all_test.go` covering by default only the worktree refusal (its own throwaway container still running afterwards, exit 6) and an answered `n` (`RunWithInput(t, "n\n", ...)`, "Nothing was stopped."); the confirmed stop (asserting its own containers stopped) is gated behind **both** `HM_E2E_STOP_ALL=1` **and** `e2e.NeedsDocker`. State in the test file's own comment: enabling `HM_E2E_STOP_ALL=1` stops every running container on the machine, including the developer's own, and it must never be enabled on a shared or development daemon.
  Verify: `go test ./test/e2e/... -run StopAll` runs the default subset only (no `HM_E2E_STOP_ALL` set); confirmed-stop subset is exercised only on a disposable machine, never in this session.
- [x] 1.11 MIGRATION: update `MIGRATION.md` — `docker-stop-all` row `shell` → `go`, counter `30 de 65` → `31 de 65`, in the same commit as the `run.go` case (1.7).
  Verify: `tests/run.sh tests/unit/migration_status_test.sh` passes (checks both directions: `:54-60` and `:87-95`).
- [x] 1.12 Regression: `go test ./internal/cli -short`; `go test ./... -short` (baseline 220 in 22 packages, expect growth only, no red); `tests/run.sh unit` (baseline 612, expect a lower count from the deleted guard suite, no red); `gofmt -l ./cmd ./internal` empty; `go vet ./...` clean; `var _ commands = (*dockergento.Engine)(nil)` still compiles.
  Verify: all green, counts recorded in the commit message per house convention.
- [x] 1.13 Size measurement (reporting only — the `size:exception` is accepted, nothing to cut): `git diff --shortstat 382247c..HEAD -- . ':(exclude)openspec'`. Expected ≈610–670. **If commit 1 alone exceeds 700, stop and report to the orchestrator rather than shrink anything** — per the design's own fallback, `test/e2e/stop_all_test.go` (1.10) is the one piece additive and independent enough to become a third commit, at the cost of `dockerd.Stop` shipping with no automated coverage until it lands, in which case the manual verification (Phase 3) MUST exercise the confirmed stop.
- [x] 1.14 Commit (orchestrator-executed): `feat(docker-tools): port docker-stop-all and start -s to Go`. Files: `dockergento/ports/ports.go`, `dockergento/adapters/dockerd/engine.go`, `dockergento/core/orchestration.go`, `dockergento/app/orchestrate.go`, `dockergento/app/orchestrate_test.go`, `dockergento/app/environments_test.go`, `dockergento/dockergento.go`, `internal/cli/engine.go`, `internal/cli/orchestrate.go`, `internal/cli/docker_stop_all.go`, `internal/cli/docker_stop_all_test.go`, `internal/cli/routing_test.go`, `internal/cli/run.go`, `console/commands/docker-stop-all.sh`, `tests/integration/stop_all_guard_test.sh` (deleted), `tests/integration/go_passthrough_test.sh`, `test/e2e/stop_all_test.go`, `MIGRATION.md`.

## Phase 2: Commit 2 — `docker-compose` (RED → GREEN)

- [x] 2.1 Foundation (no RED expected): add `ComposeRunner` to `dockergento/ports/ports.go` (`Run(dir string, files []string, environment map[string]string, args []string) (int, error)`); extract `core.ComposeFiles.Paths(root string, exists func(string) bool) []string` from `composelib.load` (`dockergento/adapters/composelib/orchestrator.go:205-224`) into `dockergento/core/environment.go`, leaving the "no compose file to read in %s" error where it is (`orchestrator.go:226-228`) and making `load` call `Paths`; create `dockergento/adapters/composecli/runner.go`: `Runner{Command string}` splits `Command` with `strings.Fields` into argv, appends the `-f` file list, `--project-directory <root>`, then the caller's args untouched, stdio inherited like `dockergento/adapters/legacy/runner.go:48-50` (read-only reference); empty `Command` → `core.Refusal{Kind: "compose_missing", Code: 3}` before any subprocess is built.
  Verify: `go build ./...` succeeds; `gofmt -l ./cmd ./internal` empty.
- [x] 2.2 App RED: in `dockergento/app/orchestrate_test.go`, add a `composeRunner` fake (dir, files, environment, args recorded) and `TestWhatTheComposeRunnerIsAskedToRun` — asserting the resolved file list (base, platform overlay, worktree overlay in a worktree, proxy overlay when present, in that order — `ComposeFiles.Paths`), the project directory, the environment, and the arguments — against `Operator.Compose`, which does not exist yet.
  Verify RED: `go test ./dockergento/app -short -run TestWhatTheComposeRunnerIsAskedToRun` → **FAILS to compile**: `Operator.Compose` is undefined.
- [x] 2.3 App GREEN: add `Compose ports.ComposeRunner` to `Operator` and a thin `Compose(project core.Project, files []string, environment map[string]string, args []string) (int, error)` in `dockergento/app/orchestrate.go` that calls `o.Compose.Run` directly — no worktree refusal (`docker-compose` is not in `hm_alters_environment`).
  Verify GREEN: `go test ./dockergento/app -short -v` → **PASSES**, the new test and the package baseline (including 1.2's tests, still green).
- [x] 2.4 Facade: add `Engine.Compose(dir string, args []string) (int, error)` in `dockergento/dockergento.go`, resolving the project, building `composecli.Runner{Command: reader.ComposeCommand()}` (`dockergento/adapters/toolinfo/tooling.go:243`, read-only reference), `ComposeFiles(project).Paths`, and `Environment(project)` (`COMPOSE_PROJECT_NAME` included, as in the shell — `bin/run:169` read-only reference for the prior no-`--project-directory` behavior).
  Verify: `go build ./...` succeeds; `go test ./dockergento/... -short` green.
- [x] 2.5 CLI RED (contract): create `internal/cli/docker_compose_test.go` with `TestWhatDockerComposeAsks` (dir and arguments verbatim, `--format json` after the command name included), `TestComposeExitCodeIsPassedThrough` (fake answers a non-zero status, handler returns it unchanged, no envelope), `TestDockerComposeOutsideAProjectIsRefused` (`projectOr` refuses first, seam records nothing), `TestAMissingComposeBinaryIsRefused` (facade resolves an empty command, refusal `compose_missing` code 3 comes back before the runner is asked anything). Handler body starts as `return exitError`, expected call log empty.
  Verify RED: `go test ./internal/cli -short -run TestWhatDockerComposeAsks` → **FAILS**: the handler does not exist, so the recorded call log stays empty against the expected one.
- [x] 2.6 CLI RED (routing): extend `TestTheRouterAnswersForTheDockerTools` (`internal/cli/routing_test.go`, from 1.6) with the `docker-compose` case.
  Verify RED: `go test ./internal/cli -short -run TestTheRouterAnswersForTheDockerTools` → **FAILS for the right reason**: no `case "docker-compose"` in `run.go` yet, so it falls through to the pinned `HM_LEGACY_ROOT` exec and returns 3.
- [x] 2.7 CLI GREEN: extend `commands` in `internal/cli/engine.go` with `Compose(dir string, args []string) (int, error)`; extend `fakeEngine` to answer it; create `internal/cli/docker_compose.go`: `projectOr` then a straight passthrough returning the raw exit code, no envelope; add `case "docker-compose": return dockerCompose(rest[1:], stdout, stderr, jsonOutput)` in `internal/cli/run.go`.
  Verify GREEN: `go test ./internal/cli -short -v` → **PASSES**, all new tests from 2.5 and 2.6, plus the full pre-existing package (including Commit 1's tests).
- [x] 2.8 Shell delegation: rewrite `console/commands/docker-compose.sh` as a `console/commands/worktree.sh:18-26`-style stub (read-only reference): the `binary_missing` refusal, then `exec "$binary" docker-compose "$@"`.
  Verify: `bash -n console/commands/docker-compose.sh` parses; `tests/run.sh unit` still passes.
- [x] 2.9 Integration: in `tests/integration/worktree_environments_test.sh`, swap the bridge probe at `:284` from `docker-compose` to `compatibility` (read-only, no `case` in `run.go`, excluded from `validate_command`); leave `:172` and `:267` (`docker-compose config --format json`) as parity assertions, now reached through the new Go handler; in `tests/integration/go_passthrough_test.sh`, add "compare the shell entry point still answers for docker-compose" (`config --format json`) and the case "compose keeps the caller's streams" (`config --format json` through the binary and through the shell produce the same stdout document), plus read-only cases `config --format json` and `ps` compared shell against binary.
  Verify: `tests/run.sh tests/integration/worktree_environments_test.sh tests/integration/go_passthrough_test.sh` passes on a machine with Docker.
- [x] 2.10 MIGRATION: update `MIGRATION.md` — `docker-compose` row `shell` → `go`, counter `31 de 65` → `32 de 65`, in the same commit as the `run.go` case (2.7).
  Verify: `tests/run.sh tests/unit/migration_status_test.sh` passes.
- [x] 2.11 Regression: `go test ./internal/cli -short`; `go test ./... -short` (no red, growth expected); `tests/run.sh unit`; `gofmt -l ./cmd ./internal` empty; `go vet ./...` clean; `var _ commands = (*dockergento.Engine)(nil)` still compiles; `rg "docker-stop-all" dockergento/app/orchestrate.go` finds no `Legacy.Run`.
  Verify: all green, counts recorded in the commit message.
- [x] 2.12 Size measurement (reporting only, nothing to cut): `git diff --shortstat 382247c..HEAD -- . ':(exclude)openspec'`. Expected total ≈970–1090 (commit 1 + commit 2). Report the final number; the `size:exception` already covers it.
- [x] 2.13 Commit (orchestrator-executed): `feat(docker-tools): port docker-compose passthrough to Go`. Files: `dockergento/ports/ports.go`, `dockergento/core/environment.go`, `dockergento/adapters/composelib/orchestrator.go`, `dockergento/adapters/composecli/runner.go`, `dockergento/app/orchestrate.go`, `dockergento/app/orchestrate_test.go`, `dockergento/dockergento.go`, `internal/cli/engine.go`, `internal/cli/docker_compose.go`, `internal/cli/docker_compose_test.go`, `internal/cli/routing_test.go`, `internal/cli/run.go`, `console/commands/docker-compose.sh`, `tests/integration/worktree_environments_test.sh`, `tests/integration/go_passthrough_test.sh`, `MIGRATION.md`.

## Phase 3: Manual verification (real project, non-destructive)

- [x] 3.1 Build the pre-change binary from `382247c` and the post-Commit-2 binary from `HEAD` (`go build -o bin/hm ./cmd/hm` for each, in separate directories). Create two or three throwaway containers on a trivial image, e.g. `docker run -d --name hm-manual-check-1 alpine sleep 600` (and `-2`, `-3`). With both builds, from a directory that is **not** a registered worktree, run `docker-stop-all` answering `n` and confirm via `docker ps` that `hm-manual-check-*` (and everything else) is still running; compare the exact text output and exit code between the two builds.
  Verify: manual — both builds print the same "This stops N container(s)...", the same conditional "M of them do not belong..." line, the same "Stop them all? [y/N]: " prompt, "Nothing was stopped." on `n`, exit 0 on both.
- [ ] 3.2 Only if the developer running this verification explicitly accepts that **every running container on the machine will stop**, repeat 3.1 answering `y` with both builds and confirm `hm-manual-check-*` stopped, comparing "Stopping N container(s)" and exit codes between builds. **`rabatrepo` or any of the developer's own projects must not be up and must not be the subject of this confirmed stop** — if the developer does not accept, skip this step and record explicitly in the verification record that it was skipped, and rely on the e2e opt-in coverage (Phase 1, task 1.10) or a dedicated disposable machine instead. Then run `hm docker-compose config` and `hm docker-compose ps` (read-only, both) against a running project with both builds and compare the documents. Finally remove the throwaway `hm-manual-check-*` containers with `docker rm -f`.
  Verify: manual — documents match field-for-field between builds (aside from expected build metadata); no container other than `hm-manual-check-*` was touched; throwaway containers removed at the end.

## Notes

- No documentation task added: `data/command_descriptions.json`, `docs/docker-stop-all.md` and `bin/run` are untouched — neither command's arguments, messages or exit codes change (proposal's Non-goals; design's File changes).
- Out of scope, no tasks: narrowing what `docker-stop-all` reaches, `composelib` learning arbitrary subcommands, rewriting `bin/run`, any Bash parity suite of another command, `Engine.ComposeFiles`'s proxy-overlay behavior (already correct, per proposal's Non-goals).
- If task 1.10 (e2e) is deferred to relieve Commit 1's size, `dockerd.Stop` ships with no automated coverage until it lands, and task 3.2's confirmed-stop step becomes mandatory rather than conditional.
