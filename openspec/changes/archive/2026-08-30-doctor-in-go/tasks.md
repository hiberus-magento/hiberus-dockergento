# Tasks

- [x] Domain: findings, the summary, the memory verdict and the version comparison, all pure
- [x] Ports for the daemon and for the machine, with fakes so the diagnosis is testable without either
- [x] The runner: seventeen checks at once, bounded each, reported in one order
- [x] The two expensive answers loaded once and handed down
- [x] The seventeen checks, wording and actions identical to the shell implementation's
- [x] The certificate read as a certificate, and the machine's memory read at build time per platform
- [x] The fingerprint, matching the one the shell generator writes into AGENTS.md
- [x] `--only`, the exit code, and the report with colour on and off
- [x] Unit tests for what is now testable without a daemon
- [x] Integration test comparing both implementations, in and out of a project
- [x] `MIGRATION.md` updated, with what the command costs
