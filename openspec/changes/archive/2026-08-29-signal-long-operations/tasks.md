# Tasks

- [x] `console/components/progress.sh`: `hm_step`, `hm_start`/`hm_stop`, `hm_working`
- [x] One decision function for whether to animate, and plain lines when it says no
- [x] Elapsed time above two seconds
- [x] Apply to `db snapshot`, `db restore`, `db freeze`, `db clone`, `mysql -i`, `clean`, `verify`
- [x] Unit tests: the decision, the shapes, the captured output, the exit code
- [x] Integration test through a pseudo-terminal: it animates there and not in a pipe
- [x] `docs/progress.md`, changelog and backlog
