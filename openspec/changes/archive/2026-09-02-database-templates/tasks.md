# Tasks

- [x] The domain: addresses, volume names, what a name may contain, sizes a person reads
- [x] Adapters: the volumes, and a one-off container with volumes attached
- [x] Freeze, list, clone and drop, with the guardrails intact
- [x] The volume and the image read from the resolved configuration, never guessed
- [x] Unit tests for the domain, and an integration test against a real database
- [x] The snapshots left where they are, by subcommand, with a test that they still get there
- [x] `MIGRATION.md` updated
