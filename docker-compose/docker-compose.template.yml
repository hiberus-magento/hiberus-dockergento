{YML_VERSION}x-toolname: "hiberus-magento"

# Environment metadata stamped on every container. Values are interpolated by Compose at
# `up` time instead of being written into this file, so they stay correct without having
# to regenerate a file that is committed to the project repository.
x-hm-labels: &hm-labels
  hm.project: "${HM_PROJECT:-}"
  hm.root: "${HM_ROOT:-}"
  hm.worktree: "${HM_WORKTREE:-}"
  hm.profile: "${HM_PROFILE:-full}"
  hm.magento: "${HM_MAGENTO:-}"
  hm.version: "${HM_VERSION:-}"
  hm.agent: "${HM_AGENT:-}"

services:
  phpfpm:
    labels: *hm-labels
    image: <php_version>
    volumes: &appvolumes
      - sockdata:/sock
      - ~/.composer:/var/www/.composer:cached
      
    environment:
      PHP_IDE_CONFIG: serverName=localhost
      COMPOSER_VERSION: <composer_version>
    depends_on:
      - db
      - search
      - redis
      - mailhog
      - rabbitmq
      
  nginx:
    labels: *hm-labels
    image: <nginx_version>
    volumes: *appvolumes
    depends_on:
      - phpfpm

  db:
    labels: *hm-labels
    image: <mariadb_version>
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: magento
      MYSQL_USER: magento
      MYSQL_PASSWORD: magento
    command: [ '--max_allowed_packet=64M', '--default-authentication-plugin=mysql_native_password', '--sql_mode=STRICT_TRANS_TABLES,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION' ]
    volumes:
      - dbdata:/var/lib/mysql

  search:
    labels: *hm-labels
    image: <search_version>
    environment:
      - discovery.type=single-node
    volumes:
      - searchdata:/usr/share/elasticsearch/data
      - opensearchdata:/usr/share/opensearch/data
    ports:
      - 9200:9200
    ulimits:
      memlock:
        soft: -1
        hard: -1

  varnish:
    labels: *hm-labels
    image: <varnish_version>
    tty: true
    ports:
      - 80:6081
    depends_on:
      - nginx

  redis:
    labels: *hm-labels
    image: <redis_version>
    volumes:
      - redisdata:/data:delegated

  mailhog:
    labels: *hm-labels
    image: <mailhog_version>
    ports:
      - 8025:8025

  rabbitmq:
    labels: *hm-labels
    image: <rabbitmq_version>
    ports:
      - 5672:5672
      - 15672:15672
    environment:
      - RABBITMQ_DEFAULT_USER=user
      - RABBITMQ_DEFAULT_PASS=password
    volumes:
      - rabbitmqdata:/var/lib/rabbitmq

  hitch:
    labels: *hm-labels
    image: <hitch_version>
    ports:
      - 443:443
    depends_on:
      - varnish

volumes:
  dbdata:
  sockdata:
  searchdata:
  opensearchdata:
  redisdata:
  rabbitmqdata: